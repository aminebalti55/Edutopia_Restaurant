/*package com.example.edutopia_res.Repository;

import com.example.edutopia_res.entities.Queue;
import com.example.edutopia_res.entities.Restaurant;
import com.example.edutopia_res.entities.User;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface QueueRepository extends JpaRepository<Queue, Integer> {
 //   int countByRestaurantAndIsServedFalse(Restaurant restaurant);

   // Queue findByRestaurant(Restaurant restaurant);

  //  List<Queue> findByRestaurantOrderByQueueNumberAsc(Restaurant restaurant);

}*/